// Re-export canonical shared entity types from the shared definitions file.
// This file acts as the public surface expected by many modules that import
// from '@/lib/types/entities'. The authoritative definitions live in
// lib/shared/types/entities.ts and are re-exported here for compatibility.

export * from "@/lib/shared/types/entities";
